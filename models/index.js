const User = require('./User'); // Importing the User model from the User module
const Thought = require('./Thought'); // Importing the Thought model from the Thought module

module.exports = { User, Thought }; // Exporting an object containing all of the imported models
